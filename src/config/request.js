/**
 * 请求常规参数  
 * @param  { nonce      } // 随机字符串
 * @param  { timestamp  } // 请求时间戳 到秒
 * @param  { user_key   } // 用户私密
 * @param  { sign       } // 生成独立编码   加密方式采用 md5
 * 
 */


/**
 * 请求地址 域名 ip 
 */
export const http_url = '';


/**
 * 请求地址 私密
 * 和服务器进行 app_key 对应
 */
export const http_url_key = '';
